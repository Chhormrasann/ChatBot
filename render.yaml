services:
  - type: web
    name: ai-chatbot-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: PORT
        value: 5001
      - key: HF_API_KEY
        sync: false
